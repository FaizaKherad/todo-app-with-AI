apiVersion: v1
kind: Service
metadata:
  name: mcp-server-service
  namespace: todo-phase-v
  labels:
    app: mcp-server
spec:
  selector:
    app: mcp-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP  # Internal service only, not exposed externally