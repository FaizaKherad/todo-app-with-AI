apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
  namespace: todo-phase-v
  labels:
    app: todo-app
spec:
  selector:
    app: todo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer  # Publicly accessible service for user interaction